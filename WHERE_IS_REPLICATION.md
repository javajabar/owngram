# Где найти Replication в Supabase Dashboard

## 📍 Пошаговая инструкция:

### Шаг 1: Выйдите из Schema Visualizer
- Schema Visualizer — это только для просмотра структуры БД
- Настройки Realtime находятся в другом разделе

### Шаг 2: Найдите раздел Replication

**Вариант A (новый интерфейс):**
1. В левом меню найдите **"Database"**
2. Внутри раздела Database найдите **"Replication"**
3. Кликните на "Replication"

**Вариант B (старый интерфейс):**
1. В левом меню найдите **"Platform"**
2. Внутри раздела Platform найдите **"Replication"**
3. Кликните на "Replication"

**Вариант C (прямой поиск):**
1. В левом меню просто найдите **"Replication"**
2. Кликните на него

### Шаг 3: Найдите таблицу call_signals

После открытия Replication вы увидите:
- Список всех таблиц вашей базы данных
- Справа от каждой таблицы — переключатель (toggle switch)

Найдите в списке таблицу **`call_signals`**

### Шаг 4: Включите Realtime

- Найдите переключатель справа от `call_signals`
- Кликните на него, чтобы включить
- Переключатель должен стать **зелёным/синим** (активным)

### Шаг 5: Проверьте

После включения:
- Переключатель должен остаться включенным
- В консоли браузера должны появиться логи: `"Successfully subscribed to call signals"`

## 🔍 Если не можете найти Replication:

1. **Проверьте версию Supabase:**
   - В старых версиях может называться **"Realtime"** вместо "Replication"
   - Или находиться в разделе **"Settings"** → **"API"** → **"Realtime"**

2. **Используйте SQL (альтернатива):**
   ```sql
   ALTER PUBLICATION supabase_realtime ADD TABLE call_signals;
   ```
   Выполните эту команду в SQL Editor — это то же самое, что включить переключатель

3. **Проверьте права доступа:**
   - Убедитесь, что у вас есть права администратора проекта
   - Обычные пользователи не могут изменять настройки Replication

## ✅ После включения:

Звонки должны заработать автоматически. Проверьте в консоли браузера (F12):
- `"Successfully subscribed to call signals"` — значит всё ок
- `"CLOSED"` или ошибки — значит что-то не так

